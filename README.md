Long List
==============

Insert really long lists into HTML

## Install

    npm install long-list


## Usage

    $el.longList(
        {
            'parent': '#parent-el',
            'template': template,
            'items': items,
            'itemHeight': 22
        }
    ).on(
        'render',
        onRender
    );

